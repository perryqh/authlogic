= Authlogic for Rails 3

This is a fork of http://github.com/binarylogic/authlogic

It updates the logged_in_status.rb and session_maintenance.rb to be Rails 3 compliant (no more deprecation warnings).